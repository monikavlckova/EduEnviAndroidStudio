<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduEnvi"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".LoginStudentActivity" />

        <activity android:name=".SignupActivity" />
        <activity android:name=".ClassroomsActivity" /> <!-- TODO ze ked kliknem spat, nic sa nestane, ide to?-->
        <activity android:name=".TasksActivity" />  <!-- TODO ze ked kliknem spat, nic sa nestane, ide to?-->
        <activity android:name=".StudentActivity" /> <!-- TODO ze ked kliknem spat, nic sa nestane, ide to?-->
        <activity android:name=".StudentRatingActivity" /> <!-- TODO ze ked kliknem spat, nic sa nestane, ide to?-->

        <activity android:name=".ClassroomGroupsActivity" />
        <activity android:name=".ClassroomStudentsActivity" />
        <activity android:name=".ClassroomTasksActivity" />
        <activity android:name=".ProfileActivity" android:launchMode="singleTop" />
        <activity android:name=".TaskType1CreatingActivity" />
        <activity android:name=".TaskType2CreatingActivity" />
        <!-- <activity android:name=".TaskType3CreatingActivity" /> -->
        <activity android:name=".TaskType1Activity" />
        <activity android:name=".TaskType2Activity" />
        <!-- <activity android:name=".TaskType3Activity" /> -->

        <activity android:name=".TaskAssignedGroupsActivity" />
        <activity android:name=".TaskAssignedStudentsActivity" />
        <activity android:name=".GroupStudentsActivity" />
        <activity android:name=".GroupTasksActivity" />
        <activity android:name=".StudentGroupsActivity" />
        <activity android:name=".StudentTasksActivity" />

        <activity android:name=".TaskType1SolutionActivity" />
        <activity android:name=".TaskType2SolutionActivity" />
        <!-- <activity android:name=".TaskType3SolutionActivity" /> -->

        <activity android:name=".AssignTaskFragment" />
        <activity android:name=".ImageGalleryFragment" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>